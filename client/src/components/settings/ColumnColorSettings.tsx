// This component has been removed as we've eliminated the Reset Colors functionality
// It's kept as an empty file to avoid breaking any imports
export function ColumnColorSettings() {
  return null;
}